# API Node.js + React

## 📌 Descrição
Projeto com CRUD, autenticação JWT e integração com React.

## 🚀 Como rodar

```bash
npm install
npm run dev

Rotas
Usuários
GET /users
POST /users
PUT /users/:id
DELETE /users/:id
Tasks
GET /tasks
POST /tasks
PUT /tasks/:id
DELETE /tasks/:id
Auth
POST /auth/login

